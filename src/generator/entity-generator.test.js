const assert = require(`assert`);
const {generate} = require(`../src/generator/entity-generator`);


describe(`Generate data`, () => {
  it(`should exist key .avatar with String value`, () => {

  });
});
